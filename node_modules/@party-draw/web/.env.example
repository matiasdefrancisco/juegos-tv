# URL del servidor Socket.IO
# En desarrollo local: dejar vacío (usa window.location.hostname:3001)
# En producción: poner la URL donde esté alojado el servidor backend (ej. Render, Railway, etc.)
# VITE_SERVER_URL=https://tu-servidor-backend.onrender.com
